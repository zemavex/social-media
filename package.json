{
  "name": "vkvex",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces",
    "lint:front": "npm run lint --workspace frontend",
    "lint:back": "npm run lint --workspace backend",
    "lint:shared": "npm run lint --workspace shared",
    "build:front": "npm run build --workspace frontend",
    "build:back": "npm run build --workspace backend",
    "dev:front": "npm run dev --workspace frontend",
    "dev:back": "npm run dev --workspace backend"
  },
  "devDependencies": {
    "@eslint/js": "^9.20.0",
    "@types/node": "^22.13.1",
    "eslint": "^9.20.0",
    "globals": "^15.14.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.23.0"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "zod": "^3.24.1"
  }
}
